## Runnable passthrough
* It passes the input into the next step without modifying it
* Input-> RunnablePassthrough-> Same Input
#### It seems useless right? But it is not.
#### It is useful when we want to preserve the original input while generating additional outputs