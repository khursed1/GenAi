# Structured Output
### It help to organize the output in predefined structured form,
### Suppose we want to parse a resume then we will need structured op like name: alam, age : 22
### we first define the structure using pydantic then we use with_Structure op to get str op from llm